Cell 30: len=1007, first80='# from huggingface_hub import HfApi, create_repo\n# import torch\n# import os\n\n# #'
Cell 31: len=61, first80='### 3.7 Verify Dynamic Temperature Scaling & Memory Footprint'
Cell 32: len=334, first80='### Memory-Optimized Model Re-instantiation\nIn this step, we clear the previous '
Cell 33: len=2046, first80='import gc\nimport torch\nfrom accelerate import init_empty_weights\nfrom transforme'
Cell 34: len=493, first80='from transformers import AutoConfig\nimport json\n\nprint(f"🔍 Inspecting raw config'
Cell 35: len=880, first80='import torch\nfrom huggingface_hub import hf_hub_download\nfrom huggingface_hub im'
Cell 36: len=1278, first80='import torch\nfrom huggingface_hub import hf_hub_download\nfrom huggingface_hub im'
Cell 37: len=535, first80='# Final Quality Validation\nprint("📊 Running Perplexity Evaluation on WikiText-2.'
Cell 38: len=375, first80='import gc\nimport torch\n\n# Flush memory pool after weight migration\nprint(f"Befor'
Cell 39: len=534, first80='from transformers import AutoTokenizer\n\n# Re-initialize tokenizer lost during re'
Cell 40: len=272, first80='if \'quantized_layers_metadata\' not in globals():\n    print("☑ Metadata was lost.'
Cell 41: len=132, first80='<a id="4"></a>\n## 4. Evaluation\n\nWe evaluate on three axes: **perplexity** (prim'
Cell 42: len=1815, first80='import gc\nimport math\nimport torch\nimport torch.nn.functional as F\nfrom tqdm.aut'
Cell 43: len=1682, first80='import gc\nimport torch\nfrom transformers import AutoModelForCausalLM, AutoTokeni'
Cell 44: len=38, first80='<a id="5"></a>\n## 5. Results Dashboard'