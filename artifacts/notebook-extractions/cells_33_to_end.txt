Total cells: 49
Cell 33: len=2038, first100='import gc\nimport torch\nfrom accelerate import init_empty_weights\nfrom transformers import AutoConfig'
Cell 34: len=493, first100='from transformers import AutoConfig\nimport json\n\nprint(f"🔍 Inspecting raw config for {MODEL_NAME}...'
Cell 35: len=535, first100='# Final Quality Validation\nprint("📊 Running Perplexity Evaluation on WikiText-2...")\n\nt0 = time.time'
Cell 36: len=375, first100='import gc\nimport torch\n\n# Flush memory pool after weight migration\nprint(f"Before cleaning: {torch.c'
Cell 37: len=534, first100='from transformers import AutoTokenizer\n\n# Re-initialize tokenizer lost during restart\nMODEL_NAME = "'
Cell 38: len=272, first100='if \'quantized_layers_metadata\' not in globals():\n    print("☑ Metadata was lost. Please re-run Secti'
Cell 39: len=132, first100='<a id="4"></a>\n## 4. Evaluation\n\nWe evaluate on three axes: **perplexity** (primary), **downstream b'
Cell 40: len=1815, first100='import gc\nimport math\nimport torch\nimport torch.nn.functional as F\nfrom tqdm.auto import tqdm\nfrom d'
Cell 41: len=1682, first100='import gc\nimport torch\nfrom transformers import AutoModelForCausalLM, AutoTokenizer\n\ntest_model_name'
Cell 42: len=38, first100='<a id="5"></a>\n## 5. Results Dashboard'
Cell 43: len=935, first100='import matplotlib.pyplot as plt\n\n# Dashboard visualizing FABQ-RC results\nfig, axes = plt.subplots(1,'
Cell 44: len=253, first100='### 5.1 Precision Distribution across Model Depth\n\nThis visualization shows where the model is alloc'
Cell 45: len=1002, first100='import matplotlib.patches as mpatches\n\n# Prepare data for precision heatmap\nlayer_precision_data = ['
Cell 46: len=476, first100='### Key Results Summary\n\n| Metric | FABQ-RC | Q1_0_g128 | BiLLM (70B) |\n|--------|---------|--------'
Cell 47: len=90, first100='The `model` object now contains the FABQ-RC quantized layers, ready for direct evaluation.'
Cell 48: len=891, first100='---\n\n## Conclusion\n\nFABQ-RC demonstrates that **adaptive per-layer blocksize** is the biggest untapp'