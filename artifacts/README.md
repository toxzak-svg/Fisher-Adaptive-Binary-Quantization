# Artifacts

Preserved scratch artifacts moved out of the repo root.

- `notebook-extractions/` contains extracted notebook cells, summaries, and
  intermediate text dumps.
- `scratch/` contains tiny one-off result files.

These files are kept for auditability. They should not be treated as canonical
experiment results; use `results/` for benchmark outputs.
