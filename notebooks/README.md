# Notebooks

- `archive/` contains the former root-level research notebooks.
- `latest/` contains the former `latest notebooks/` folder contents.
- Root files in this folder are active notebook builders or current notebook
  variants.

The archived notebooks are preserved for reproducibility and history. Prefer
new scripts or benchmarks for repeatable validation work.
