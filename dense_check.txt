Cell 11: len=4014, first120='class FisherAccumulator:\n    """Accumulate Fisher Information (gradient² proxy) per output channel with device awareness'
Cell 12: len=3010, first120='import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\nclass QuantizedLinear(nn.Module):\n    """A linear la'
Cell 13: len=3708, first120='\n# ========================================\n# FABQ-RC PROPER SAVE/LOAD FUNCTIONS\n# ====================================='
Cell 14: len=1199, first120='class DynamicResidualCodebook(nn.Module):\n    """Dynamic residual codebook that applies routing-aware scaling."""\n    de'
Cell 15: len=1920, first120='import matplotlib.pyplot as plt\nimport numpy as np\n\nlayer_names = list(fisher_scores.keys())\nfisher_vals = [f.abs().mean'
Cell 16: len=135, first120='### 3.3 Stage 2 — Mixed-Precision Core Allocation\n\nTop 5% Fisher channels → int8 (preserve accuracy). Bottom 95% → binar'
Cell 17: len=1493, first120='INT4_FRACTION = 0.05  # Keep top 5% Fisher channels as int4\n\ndef allocate_precision(fisher_dict, int4_fraction=0.05):\n  '
Cell 19: len=1998, first120='BS_CANDIDATES = [64, 128, 256, 512]\n# Penalty for smaller blocks to encourage compression efficiency\nBS_PENALTIES = {16:'
Cell 22: len=2737, first120='def build_codebook(model, allocation, blocksize_results, cal_loader, device, n_clusters=256, max_samples=8192):\n    """\n'